apiVersion: v1
kind: Service
metadata:
  name: ui-service
spec:
  selector:
    app: newbank-ui
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007
  type: NodePort
